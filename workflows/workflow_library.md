# 🔁 Workflow Library

Reusable workflows generated by Workflow Builder Agent.

---

## 1. Full Project Build

```
Planning → Stack → Repo → DB → Backend → API → Frontend → UI → Design → Data → Marketing → SEO → Content → Social → Analytics → Monetization → Tasks
```

**Skills chain**: `project-planner` → `stack-verifier` → `database-designer` → `backend-builder` → `api-designer` → `frontend-builder` → `deployment-planner` → `documentation-generator`

See [`project-startup.md`](./project-startup.md) for the detailed step-by-step.

---

## 2. Content Engine Workflow
```
Content Agent
→ SEO Agent
→ Content Calendar Agent
→ Social Media Agent
→ Analytics Agent
```

---

## 3. Social Growth Workflow
```
Campaign Planner
→ Content Agent
→ Social Size Chart Agent
→ Social Media Integration Agent
→ Social Agent
→ Analytics Agent
```

---

## 4. Data Pipeline Workflow
```
Data/AI Agent
→ Database Agent
→ API Agent
→ Analytics Agent
```

---

## 5. Monetization Workflow
```
Product Features
→ Monetization Agent
→ Campaign Planner
→ Marketing Agent
→ Analytics Agent
```

---

## 6. Skill System Workflow

### 6a. Skill Creation Loop
```
Capture intent (skill-creator)
→ Draft SKILL.md (skill-creator)
→ Write eval cases (eval-runner)
→ Run evals (eval-runner)
→ Evaluate results (eval-runner)
→ Improve skill (skill-creator)
→ Re-run evals until quality gates met
→ Optimize description (skill-creator)
→ Publish skill
```

See [`skill-lifecycle.md`](./skill-lifecycle.md) for the full lifecycle documentation.

### 6b. Plan → Build → Deploy Chain
```
project-planner
→ stack-verifier
→ database-designer
→ backend-builder
→ api-designer
→ frontend-builder
→ webapp-testing
→ deployment-planner
→ documentation-generator
```

Each skill in this chain has a corresponding workflow doc:
- [`project-startup.md`](./project-startup.md)
- [`stack-identification.md`](./stack-identification.md)
- [`database-build.md`](./database-build.md)
- [`backend-build.md`](./backend-build.md)
- [`api-build.md`](./api-build.md)
- [`frontend-build.md`](./frontend-build.md)
- [`testing-review.md`](./testing-review.md)
- [`deployment.md`](./deployment.md)
- [`fullstack-integration.md`](./fullstack-integration.md)

---

## Skill Index

Active skill folders in `skills/`:

| Skill | Category | Description |
|-------|---------|-------------|
| `project-planner` | planning | Convert ideas into structured project plans |
| `planning-agent-skill` | planning | Detailed planning with module maps and roadmaps |
| `project-planning-skill` | planning | Transform ideas into structured project briefs |
| `stack-verifier` | verification | Verify dev environment and stack setup |
| `database-designer` | build | Design PostgreSQL schemas and migrations |
| `backend-builder` | build | Build FastAPI routes, services, and models |
| `api-designer` | build | Design REST/GraphQL API contracts |
| `frontend-builder` | build | Build React components and pages |
| `webapp-testing` | testing | Test web apps with Playwright |
| `eval-runner` | evaluation | Create and run skill evals |
| `deployment-planner` | deployment | Plan and execute deployments |
| `documentation-generator` | documentation | Generate and maintain project docs |
| `metrics-definition-skill` | analytics | Define KPIs and tracking events |
| `skill-creator` | meta | Create and improve skills |

