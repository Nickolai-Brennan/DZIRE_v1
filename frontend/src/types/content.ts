// Re-export all types from the canonical data types file
// to ensure compatibility across the codebase
export type {
  Position,
  Review,
  DictionaryTerm,
  DollProfile,
  Story,
  MagazineIssue,
  AdminStats,
} from '../data/types';
