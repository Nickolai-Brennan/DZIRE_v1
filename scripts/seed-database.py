#!/usr/bin/env python3
"""seed-database.py — Insert starter data into the DZIRE_v1 PostgreSQL database."""

import os

print("==> Seeding DZIRE_v1 database...")
print("    Add seed scripts to database/seeds/ and import them here.")
print("    DATABASE_URL:", os.environ.get("DATABASE_URL", "(not set)"))
print("==> Done.")
